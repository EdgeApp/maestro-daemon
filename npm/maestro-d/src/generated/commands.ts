// Code generated by go run ./pkg/daemon/gen from pkg/flow; DO NOT EDIT.

import type { Condition, Selector, Step } from '../types'

/** Accepts a system alert dialog (taps Allow/OK). */
export interface AcceptAlertParams {
  optional?: boolean
  label?: string
  timeout?: number
  /** Platform restricts this step to a single platform; when set and it doesn't match the running driver, the step is skipped (Maestro #1353). */
  platform?: string
}

/** Adds media files. */
export interface AddMediaParams {
  optional?: boolean
  label?: string
  timeout?: number
  /** Platform restricts this step to a single platform; when set and it doesn't match the running driver, the step is skipped (Maestro #1353). */
  platform?: string
  files?: string[]
}

/** Asserts a condition. Uses a custom UnmarshalYAML because both BaseStep and Condition have a "timeout" yaml tag — Condition.Timeout is the semantically correct one here (controls how long to wait for visible/notVisible condition checks). */
export interface AssertConditionParams {
  visible?: Selector
  notVisible?: Selector
  true?: string
  platform?: string
  /** Timeout in ms for visible/notVisible checks */
  timeout?: number
}

/** Asserts the system UI is currently in dark appearance. */
export interface AssertDarkModeParams {
  optional?: boolean
  label?: string
  timeout?: number
  /** Platform restricts this step to a single platform; when set and it doesn't match the running driver, the step is skipped (Maestro #1353). */
  platform?: string
}

/** Asserts the system UI is currently in light appearance. */
export interface AssertLightModeParams {
  optional?: boolean
  label?: string
  timeout?: number
  /** Platform restricts this step to a single platform; when set and it doesn't match the running driver, the step is skipped (Maestro #1353). */
  platform?: string
}

/** Uses AI to check for visual defects. */
export interface AssertNoDefectsWithAIParams {
  optional?: boolean
  label?: string
  timeout?: number
  /** Platform restricts this step to a single platform; when set and it doesn't match the running driver, the step is skipped (Maestro #1353). */
  platform?: string
}

/** Asserts that no console errors or uncaught exceptions occurred. */
export interface AssertNoJSErrorsParams {
  optional?: boolean
  label?: string
  timeout?: number
  /** Platform restricts this step to a single platform; when set and it doesn't match the running driver, the step is skipped (Maestro #1353). */
  platform?: string
}

/** Asserts element is not visible. */
export interface AssertNotVisibleParams {
  optional?: boolean
  label?: string
  timeout?: number
  /** Platform restricts this step to a single platform; when set and it doesn't match the running driver, the step is skipped (Maestro #1353). */
  platform?: string
  /** Primary selectors */
  text?: string
  /** Resource ID or accessibility ID */
  id?: string
  /** Size matching */
  width?: number
  height?: number
  tolerance?: number
  /** State filters */
  enabled?: boolean
  selected?: boolean
  checked?: boolean
  focused?: boolean
  /** Index for multiple matches (string for variable support) */
  index?: string
  /** Traits (comma-separated string, e.g., "button,heading") */
  traits?: string
  /** CSS selector for web views */
  css?: string
  /** Web-specific selectors */
  placeholder?: string
  /** Match by ARIA role (button, link, tab, etc.) */
  role?: string
  /** Partial text match (contains) */
  textContains?: string
  /** Match links by href attribute */
  href?: string
  /** Match by alt attribute (images) */
  alt?: string
  /** Match by title attribute (tooltips) */
  title?: string
  /** Match by form field name attribute */
  name?: string
  /** Match by data-testid attribute */
  testId?: string
  /** Match text by regex pattern */
  textRegex?: string
  /** Pick Nth match (0-based) when multiple elements match */
  nth?: number
  /** Relative selectors */
  childOf?: Selector
  below?: Selector
  above?: Selector
  leftOf?: Selector
  rightOf?: Selector
  containsChild?: Selector
  containsDescendants?: Selector[]
  /** Visual containment (center point inside anchor bounds) */
  insideOf?: Selector
  retryTapIfNoChange?: boolean
  waitUntilVisible?: boolean
  /** Tap point "x%, y%" */
  point?: string
  /** Swipe start "x%, y%" */
  start?: string
  /** Swipe end "x%, y%" */
  end?: string
  /** Tap repeat count */
  repeat?: number
  /** Delay between repeats (ms) */
  delay?: number
  /** Wait for UI settle (ms) */
  waitToSettleTimeoutMs?: number
}

/** Compares a screenshot with a reference image. */
export interface AssertScreenshotParams {
  optional?: boolean
  label?: string
  timeout?: number
  /** Platform restricts this step to a single platform; when set and it doesn't match the running driver, the step is skipped (Maestro #1353). */
  platform?: string
  path?: string
  cropOn?: Selector
  thresholdPercentage?: unknown
}

/** Asserts a script condition is true (alias for assertCondition). */
export interface AssertTrueParams {
  optional?: boolean
  label?: string
  timeout?: number
  /** Platform restricts this step to a single platform; when set and it doesn't match the running driver, the step is skipped (Maestro #1353). */
  platform?: string
  condition?: string
}

/** Asserts element is visible. */
export interface AssertVisibleParams {
  optional?: boolean
  label?: string
  timeout?: number
  /** Platform restricts this step to a single platform; when set and it doesn't match the running driver, the step is skipped (Maestro #1353). */
  platform?: string
  /** Primary selectors */
  text?: string
  /** Resource ID or accessibility ID */
  id?: string
  /** Size matching */
  width?: number
  height?: number
  tolerance?: number
  /** State filters */
  enabled?: boolean
  selected?: boolean
  checked?: boolean
  focused?: boolean
  /** Index for multiple matches (string for variable support) */
  index?: string
  /** Traits (comma-separated string, e.g., "button,heading") */
  traits?: string
  /** CSS selector for web views */
  css?: string
  /** Web-specific selectors */
  placeholder?: string
  /** Match by ARIA role (button, link, tab, etc.) */
  role?: string
  /** Partial text match (contains) */
  textContains?: string
  /** Match links by href attribute */
  href?: string
  /** Match by alt attribute (images) */
  alt?: string
  /** Match by title attribute (tooltips) */
  title?: string
  /** Match by form field name attribute */
  name?: string
  /** Match by data-testid attribute */
  testId?: string
  /** Match text by regex pattern */
  textRegex?: string
  /** Pick Nth match (0-based) when multiple elements match */
  nth?: number
  /** Relative selectors */
  childOf?: Selector
  below?: Selector
  above?: Selector
  leftOf?: Selector
  rightOf?: Selector
  containsChild?: Selector
  containsDescendants?: Selector[]
  /** Visual containment (center point inside anchor bounds) */
  insideOf?: Selector
  retryTapIfNoChange?: boolean
  waitUntilVisible?: boolean
  /** Tap point "x%, y%" */
  point?: string
  /** Swipe start "x%, y%" */
  start?: string
  /** Swipe end "x%, y%" */
  end?: string
  /** Tap repeat count */
  repeat?: number
  /** Delay between repeats (ms) */
  delay?: number
  /** Wait for UI settle (ms) */
  waitToSettleTimeoutMs?: number
  /** Count asserts that the selector matches exactly N visible elements (Maestro #1363). A string so flows can write `count: ${N}`; empty means the ordinary at-least-one assertion. */
  count?: string
}

/** Uses AI to verify an assertion. */
export interface AssertWithAIParams {
  optional?: boolean
  label?: string
  timeout?: number
  /** Platform restricts this step to a single platform; when set and it doesn't match the running driver, the step is skipped (Maestro #1353). */
  platform?: string
  assertion?: string
}

/** Presses back. */
export interface BackParams {
  optional?: boolean
  label?: string
  timeout?: number
  /** Platform restricts this step to a single platform; when set and it doesn't match the running driver, the step is skipped (Maestro #1353). */
  platform?: string
}

/** Blocks network requests matching URL patterns. */
export interface BlockNetworkParams {
  optional?: boolean
  label?: string
  timeout?: number
  /** Platform restricts this step to a single platform; when set and it doesn't match the running driver, the step is skipped (Maestro #1353). */
  platform?: string
  patterns?: string[]
}

/** Clears captured browser console logs. */
export interface ClearConsoleLogsParams {
  optional?: boolean
  label?: string
  timeout?: number
  /** Platform restricts this step to a single platform; when set and it doesn't match the running driver, the step is skipped (Maestro #1353). */
  platform?: string
}

/** Clears keychain. */
export interface ClearKeychainParams {
  optional?: boolean
  label?: string
  timeout?: number
  /** Platform restricts this step to a single platform; when set and it doesn't match the running driver, the step is skipped (Maestro #1353). */
  platform?: string
}

/** Clears all network mocks and blocks. */
export interface ClearNetworkMocksParams {
  optional?: boolean
  label?: string
  timeout?: number
  /** Platform restricts this step to a single platform; when set and it doesn't match the running driver, the step is skipped (Maestro #1353). */
  platform?: string
}

/** Clears app state. */
export interface ClearStateParams {
  optional?: boolean
  label?: string
  timeout?: number
  /** Platform restricts this step to a single platform; when set and it doesn't match the running driver, the step is skipped (Maestro #1353). */
  platform?: string
  appId?: string
}

/** Closes the current tab and switches to the previous one. */
export interface CloseTabParams {
  optional?: boolean
  label?: string
  timeout?: number
  /** Platform restricts this step to a single platform; when set and it doesn't match the running driver, the step is skipped (Maestro #1353). */
  platform?: string
}

/** Copies text from element. */
export interface CopyTextFromParams {
  optional?: boolean
  label?: string
  timeout?: number
  /** Platform restricts this step to a single platform; when set and it doesn't match the running driver, the step is skipped (Maestro #1353). */
  platform?: string
  /** Primary selectors */
  text?: string
  /** Resource ID or accessibility ID */
  id?: string
  /** Size matching */
  width?: number
  height?: number
  tolerance?: number
  /** State filters */
  enabled?: boolean
  selected?: boolean
  checked?: boolean
  focused?: boolean
  /** Index for multiple matches (string for variable support) */
  index?: string
  /** Traits (comma-separated string, e.g., "button,heading") */
  traits?: string
  /** CSS selector for web views */
  css?: string
  /** Web-specific selectors */
  placeholder?: string
  /** Match by ARIA role (button, link, tab, etc.) */
  role?: string
  /** Partial text match (contains) */
  textContains?: string
  /** Match links by href attribute */
  href?: string
  /** Match by alt attribute (images) */
  alt?: string
  /** Match by title attribute (tooltips) */
  title?: string
  /** Match by form field name attribute */
  name?: string
  /** Match by data-testid attribute */
  testId?: string
  /** Match text by regex pattern */
  textRegex?: string
  /** Pick Nth match (0-based) when multiple elements match */
  nth?: number
  /** Relative selectors */
  childOf?: Selector
  below?: Selector
  above?: Selector
  leftOf?: Selector
  rightOf?: Selector
  containsChild?: Selector
  containsDescendants?: Selector[]
  /** Visual containment (center point inside anchor bounds) */
  insideOf?: Selector
  retryTapIfNoChange?: boolean
  waitUntilVisible?: boolean
  /** Tap point "x%, y%" */
  point?: string
  /** Swipe start "x%, y%" */
  start?: string
  /** Swipe end "x%, y%" */
  end?: string
  /** Tap repeat count */
  repeat?: number
  /** Delay between repeats (ms) */
  delay?: number
  /** Wait for UI settle (ms) */
  waitToSettleTimeoutMs?: number
}

/** Defines session variables; the value is a map of NAME: value. */
export type DefineVariablesParams = Record<string, string>

/** Dismisses a system alert dialog (taps Don't Allow/Cancel). */
export interface DismissAlertParams {
  optional?: boolean
  label?: string
  timeout?: number
  /** Platform restricts this step to a single platform; when set and it doesn't match the running driver, the step is skipped (Maestro #1353). */
  platform?: string
}

/** Double taps on an element (alias for tapOn with repeat=2). */
export interface DoubleTapOnParams {
  optional?: boolean
  label?: string
  timeout?: number
  /** Platform restricts this step to a single platform; when set and it doesn't match the running driver, the step is skipped (Maestro #1353). */
  platform?: string
  /** Primary selectors */
  text?: string
  /** Resource ID or accessibility ID */
  id?: string
  /** Size matching */
  width?: number
  height?: number
  tolerance?: number
  /** State filters */
  enabled?: boolean
  selected?: boolean
  checked?: boolean
  focused?: boolean
  /** Index for multiple matches (string for variable support) */
  index?: string
  /** Traits (comma-separated string, e.g., "button,heading") */
  traits?: string
  /** CSS selector for web views */
  css?: string
  /** Web-specific selectors */
  placeholder?: string
  /** Match by ARIA role (button, link, tab, etc.) */
  role?: string
  /** Partial text match (contains) */
  textContains?: string
  /** Match links by href attribute */
  href?: string
  /** Match by alt attribute (images) */
  alt?: string
  /** Match by title attribute (tooltips) */
  title?: string
  /** Match by form field name attribute */
  name?: string
  /** Match by data-testid attribute */
  testId?: string
  /** Match text by regex pattern */
  textRegex?: string
  /** Pick Nth match (0-based) when multiple elements match */
  nth?: number
  /** Relative selectors */
  childOf?: Selector
  below?: Selector
  above?: Selector
  leftOf?: Selector
  rightOf?: Selector
  containsChild?: Selector
  containsDescendants?: Selector[]
  /** Visual containment (center point inside anchor bounds) */
  insideOf?: Selector
  retryTapIfNoChange?: boolean
  waitUntilVisible?: boolean
  /** Tap point "x%, y%" */
  point?: string
  /** Swipe start "x%, y%" */
  start?: string
  /** Swipe end "x%, y%" */
  end?: string
  /** Tap repeat count */
  repeat?: number
  /** Delay between repeats (ms) */
  delay?: number
  /** Wait for UI settle (ms) */
  waitToSettleTimeoutMs?: number
}

/** SwipeStep performs a swipe gesture. */
export interface DragAndDropParams {
  optional?: boolean
  label?: string
  timeout?: number
  /** Platform restricts this step to a single platform; when set and it doesn't match the running driver, the step is skipped (Maestro #1353). */
  platform?: string
  /** what to pick up — selector or point */
  from?: Selector
  /** where to drop it — selector or point */
  to?: Selector
  /** HoldDuration is how long to press before moving (ms). Reorder UIs lift the item only after a long press, so the default is a full second. */
  holdDuration?: number
  /** Duration is how long the movement itself takes (ms). Drag targets track the finger, so moving too fast skips drop zones; default one second. */
  duration?: number
}

/** Erases text. */
export interface EraseTextParams {
  optional?: boolean
  label?: string
  timeout?: number
  /** Platform restricts this step to a single platform; when set and it doesn't match the running driver, the step is skipped (Maestro #1353). */
  platform?: string
  characters?: number
}

/** Executes JavaScript in the browser page context (web only). Unlike EvalScriptStep which runs in Maestro's internal JS engine, this runs directly in the browser via CDP, with access to window, document, DOM, etc. */
export interface EvalBrowserScriptParams {
  optional?: boolean
  label?: string
  timeout?: number
  /** Platform restricts this step to a single platform; when set and it doesn't match the running driver, the step is skipped (Maestro #1353). */
  platform?: string
  /** JS code to execute in the browser */
  script?: string
  /** Variable name to store the return value */
  output?: string
}

/** Evaluates JavaScript. */
export interface EvalScriptParams {
  optional?: boolean
  label?: string
  timeout?: number
  /** Platform restricts this step to a single platform; when set and it doesn't match the running driver, the step is skipped (Maestro #1353). */
  platform?: string
  script?: string
}

/** Executes JavaScript in a mobile WebView via CDP. Unlike EvalBrowserScriptStep (desktop browser), this runs in the WebView context of a native Android app. */
export interface EvalWebViewScriptParams {
  optional?: boolean
  label?: string
  timeout?: number
  /** Platform restricts this step to a single platform; when set and it doesn't match the running driver, the step is skipped (Maestro #1353). */
  platform?: string
  /** JS code to execute in the WebView */
  script?: string
  /** Variable name to store the return value */
  output?: string
}

/** Waits for a condition. */
export interface ExtendedWaitUntilParams {
  optional?: boolean
  label?: string
  timeout?: number
  /** Platform restricts this step to a single platform; when set and it doesn't match the running driver, the step is skipped (Maestro #1353). */
  platform?: string
  visible?: Selector
  notVisible?: Selector
}

/** Uses AI to extract text from screen. */
export interface ExtractTextWithAIParams {
  optional?: boolean
  label?: string
  timeout?: number
  /** Platform restricts this step to a single platform; when set and it doesn't match the running driver, the step is skipped (Maestro #1353). */
  platform?: string
  query?: string
  /** Variable to store result */
  variable?: string
}

/** Retrieves captured browser console logs as JSON. */
export interface GetConsoleLogsParams {
  optional?: boolean
  label?: string
  timeout?: number
  /** Platform restricts this step to a single platform; when set and it doesn't match the running driver, the step is skipped (Maestro #1353). */
  platform?: string
  /** Variable name to store JSON result */
  output?: string
}

/** Retrieves browser cookies and stores them as JSON. */
export interface GetCookiesParams {
  optional?: boolean
  label?: string
  timeout?: number
  /** Platform restricts this step to a single platform; when set and it doesn't match the running driver, the step is skipped (Maestro #1353). */
  platform?: string
  /** Variable name to store JSON result */
  output?: string
}

/** Grants browser permissions (notifications, camera, etc). */
export interface GrantPermissionsParams {
  optional?: boolean
  label?: string
  timeout?: number
  /** Platform restricts this step to a single platform; when set and it doesn't match the running driver, the step is skipped (Maestro #1353). */
  platform?: string
  permissions?: string[]
  /** Optional: specific origin */
  origin?: string
}

/** Hides the keyboard. */
export interface HideKeyboardParams {
  optional?: boolean
  label?: string
  timeout?: number
  /** Platform restricts this step to a single platform; when set and it doesn't match the running driver, the step is skipped (Maestro #1353). */
  platform?: string
}

/** Generates random input. */
export interface InputRandomParams {
  optional?: boolean
  label?: string
  timeout?: number
  /** Platform restricts this step to a single platform; when set and it doesn't match the running driver, the step is skipped (Maestro #1353). */
  platform?: string
  /** TEXT, NUMBER, EMAIL, PERSON_NAME, etc. */
  type?: string
  length?: number
}

/** Generates random input. */
export interface InputRandomEmailParams {
  optional?: boolean
  label?: string
  timeout?: number
  /** Platform restricts this step to a single platform; when set and it doesn't match the running driver, the step is skipped (Maestro #1353). */
  platform?: string
  /** TEXT, NUMBER, EMAIL, PERSON_NAME, etc. */
  type?: string
  length?: number
}

/** Generates random input. */
export interface InputRandomNumberParams {
  optional?: boolean
  label?: string
  timeout?: number
  /** Platform restricts this step to a single platform; when set and it doesn't match the running driver, the step is skipped (Maestro #1353). */
  platform?: string
  /** TEXT, NUMBER, EMAIL, PERSON_NAME, etc. */
  type?: string
  length?: number
}

/** Generates random input. */
export interface InputRandomPersonNameParams {
  optional?: boolean
  label?: string
  timeout?: number
  /** Platform restricts this step to a single platform; when set and it doesn't match the running driver, the step is skipped (Maestro #1353). */
  platform?: string
  /** TEXT, NUMBER, EMAIL, PERSON_NAME, etc. */
  type?: string
  length?: number
}

/** Generates random input. */
export interface InputRandomTextParams {
  optional?: boolean
  label?: string
  timeout?: number
  /** Platform restricts this step to a single platform; when set and it doesn't match the running driver, the step is skipped (Maestro #1353). */
  platform?: string
  /** TEXT, NUMBER, EMAIL, PERSON_NAME, etc. */
  type?: string
  length?: number
}

/** Inputs text. */
export interface InputTextParams {
  optional?: boolean
  label?: string
  timeout?: number
  /** Platform restricts this step to a single platform; when set and it doesn't match the running driver, the step is skipped (Maestro #1353). */
  platform?: string
  text?: string
  /** If true, simulate real key presses (Android native only) */
  keyPress?: boolean
  /** Resource ID or accessibility ID */
  id?: string
  /** Size matching */
  width?: number
  height?: number
  tolerance?: number
  /** State filters */
  enabled?: boolean
  selected?: boolean
  checked?: boolean
  focused?: boolean
  /** Index for multiple matches (string for variable support) */
  index?: string
  /** Traits (comma-separated string, e.g., "button,heading") */
  traits?: string
  /** CSS selector for web views */
  css?: string
  /** Web-specific selectors */
  placeholder?: string
  /** Match by ARIA role (button, link, tab, etc.) */
  role?: string
  /** Partial text match (contains) */
  textContains?: string
  /** Match links by href attribute */
  href?: string
  /** Match by alt attribute (images) */
  alt?: string
  /** Match by title attribute (tooltips) */
  title?: string
  /** Match by form field name attribute */
  name?: string
  /** Match by data-testid attribute */
  testId?: string
  /** Match text by regex pattern */
  textRegex?: string
  /** Pick Nth match (0-based) when multiple elements match */
  nth?: number
  /** Relative selectors */
  childOf?: Selector
  below?: Selector
  above?: Selector
  leftOf?: Selector
  rightOf?: Selector
  containsChild?: Selector
  containsDescendants?: Selector[]
  /** Visual containment (center point inside anchor bounds) */
  insideOf?: Selector
  retryTapIfNoChange?: boolean
  waitUntilVisible?: boolean
  /** Tap point "x%, y%" */
  point?: string
  /** Swipe start "x%, y%" */
  start?: string
  /** Swipe end "x%, y%" */
  end?: string
  /** Tap repeat count */
  repeat?: number
  /** Delay between repeats (ms) */
  delay?: number
  /** Wait for UI settle (ms) */
  waitToSettleTimeoutMs?: number
}

/** Kills an app. */
export interface KillAppParams {
  optional?: boolean
  label?: string
  timeout?: number
  /** Platform restricts this step to a single platform; when set and it doesn't match the running driver, the step is skipped (Maestro #1353). */
  platform?: string
  appId?: string
}

/** Launches an app. */
export interface LaunchAppParams {
  optional?: boolean
  label?: string
  timeout?: number
  /** Platform restricts this step to a single platform; when set and it doesn't match the running driver, the step is skipped (Maestro #1353). */
  platform?: string
  appId?: string
  clearState?: boolean
  clearKeychain?: boolean
  stopApp?: boolean
  /** Appium only: create fresh session */
  newSession?: boolean
  permissions?: Record<string, string>
  /** Launch arguments (-key value pairs) */
  arguments?: unknown
  /** Launch environment variables */
  environment?: Record<string, string>
}

/** Loads cookies + localStorage + sessionStorage from a JSON file. */
export interface LoadAuthStateParams {
  optional?: boolean
  label?: string
  timeout?: number
  /** Platform restricts this step to a single platform; when set and it doesn't match the running driver, the step is skipped (Maestro #1353). */
  platform?: string
  /** Input file path */
  path?: string
}

/** Long presses on an element (alias for tapOn with longPress=true). */
export interface LongPressOnParams {
  optional?: boolean
  label?: string
  timeout?: number
  /** Platform restricts this step to a single platform; when set and it doesn't match the running driver, the step is skipped (Maestro #1353). */
  platform?: string
  /** Primary selectors */
  text?: string
  /** Resource ID or accessibility ID */
  id?: string
  /** Size matching */
  width?: number
  height?: number
  tolerance?: number
  /** State filters */
  enabled?: boolean
  selected?: boolean
  checked?: boolean
  focused?: boolean
  /** Index for multiple matches (string for variable support) */
  index?: string
  /** Traits (comma-separated string, e.g., "button,heading") */
  traits?: string
  /** CSS selector for web views */
  css?: string
  /** Web-specific selectors */
  placeholder?: string
  /** Match by ARIA role (button, link, tab, etc.) */
  role?: string
  /** Partial text match (contains) */
  textContains?: string
  /** Match links by href attribute */
  href?: string
  /** Match by alt attribute (images) */
  alt?: string
  /** Match by title attribute (tooltips) */
  title?: string
  /** Match by form field name attribute */
  name?: string
  /** Match by data-testid attribute */
  testId?: string
  /** Match text by regex pattern */
  textRegex?: string
  /** Pick Nth match (0-based) when multiple elements match */
  nth?: number
  /** Relative selectors */
  childOf?: Selector
  below?: Selector
  above?: Selector
  leftOf?: Selector
  rightOf?: Selector
  containsChild?: Selector
  containsDescendants?: Selector[]
  /** Visual containment (center point inside anchor bounds) */
  insideOf?: Selector
  retryTapIfNoChange?: boolean
  waitUntilVisible?: boolean
  /** Tap point "x%, y%" */
  point?: string
  /** Swipe start "x%, y%" */
  start?: string
  /** Swipe end "x%, y%" */
  end?: string
  /** Tap repeat count */
  repeat?: number
  /** Delay between repeats (ms) */
  delay?: number
  /** Wait for UI settle (ms) */
  waitToSettleTimeoutMs?: number
  duration?: number
}

/** Mocks API responses matching URL pattern and method. */
export interface MockNetworkParams {
  optional?: boolean
  label?: string
  timeout?: number
  /** Platform restricts this step to a single platform; when set and it doesn't match the running driver, the step is skipped (Maestro #1353). */
  platform?: string
  url?: string
  /** GET, POST, etc. (empty = match all) */
  method?: string
  response?: unknown
}

/** Opens a URL in the browser. */
export interface OpenBrowserParams {
  optional?: boolean
  label?: string
  timeout?: number
  /** Platform restricts this step to a single platform; when set and it doesn't match the running driver, the step is skipped (Maestro #1353). */
  platform?: string
  url?: string
}

/** Opens a URL. */
export interface OpenLinkParams {
  optional?: boolean
  label?: string
  timeout?: number
  /** Platform restricts this step to a single platform; when set and it doesn't match the running driver, the step is skipped (Maestro #1353). */
  platform?: string
  link?: string
  autoVerify?: boolean
  browser?: boolean
}

/** Pulls down the Android notification shade. Android-only (no-op on iOS). */
export interface OpenNotificationsParams {
  optional?: boolean
  label?: string
  timeout?: number
  /** Platform restricts this step to a single platform; when set and it doesn't match the running driver, the step is skipped (Maestro #1353). */
  platform?: string
}

/** Opens a new browser tab. */
export interface OpenTabParams {
  optional?: boolean
  label?: string
  timeout?: number
  /** Platform restricts this step to a single platform; when set and it doesn't match the running driver, the step is skipped (Maestro #1353). */
  platform?: string
  url?: string
  /** Optional name for switching back */
  tabLabel?: string
}

/** Pastes text. */
export interface PasteTextParams {
  optional?: boolean
  label?: string
  timeout?: number
  /** Platform restricts this step to a single platform; when set and it doesn't match the running driver, the step is skipped (Maestro #1353). */
  platform?: string
}

/** Presses a key. */
export interface PressKeyParams {
  optional?: boolean
  label?: string
  timeout?: number
  /** Platform restricts this step to a single platform; when set and it doesn't match the running driver, the step is skipped (Maestro #1353). */
  platform?: string
  key?: string
}

/** Clears media added by addMedia (Android: MediaStore index). */
export interface RemoveMediaParams {
  optional?: boolean
  label?: string
  timeout?: number
  /** Platform restricts this step to a single platform; when set and it doesn't match the running driver, the step is skipped (Maestro #1353). */
  platform?: string
}

/** Repeats steps. */
export interface RepeatParams {
  optional?: boolean
  label?: string
  timeout?: number
  /** Platform restricts this step to a single platform; when set and it doesn't match the running driver, the step is skipped (Maestro #1353). */
  platform?: string
  /** String for variable support */
  times?: string
  while?: Condition
  /** Steps to repeat */
  commands?: Step[]
}

/** Resets all browser permissions. */
export interface ResetPermissionsParams {
  optional?: boolean
  label?: string
  timeout?: number
  /** Platform restricts this step to a single platform; when set and it doesn't match the running driver, the step is skipped (Maestro #1353). */
  platform?: string
}

/** Retries steps on failure. */
export interface RetryParams {
  optional?: boolean
  label?: string
  timeout?: number
  /** Platform restricts this step to a single platform; when set and it doesn't match the running driver, the step is skipped (Maestro #1353). */
  platform?: string
  /** String for variable support */
  maxRetries?: string
  file?: string
  env?: Record<string, string>
  /** Steps to retry (alternative to file) */
  commands?: Step[]
}

/** Loads and executes a JS file in the browser page context. */
export interface RunBrowserScriptParams {
  optional?: boolean
  label?: string
  timeout?: number
  /** Platform restricts this step to a single platform; when set and it doesn't match the running driver, the step is skipped (Maestro #1353). */
  platform?: string
  /** Path to JS file */
  file?: string
  /** Environment variables injected as window.__env */
  env?: Record<string, string>
  /** Variable name to store the return value */
  output?: string
}

/** Runs another flow. */
export interface RunFlowParams {
  optional?: boolean
  label?: string
  timeout?: number
  /** Platform restricts this step to a single platform; when set and it doesn't match the running driver, the step is skipped (Maestro #1353). */
  platform?: string
  file?: string
  when?: Condition
  env?: Record<string, string>
  /** Inline steps (alternative to file) */
  commands?: Step[]
  /** Fallback flow file or inline steps when `when` is false */
  else?: string | Step[]
  /** Inline fallback steps when `when` is false */
  elseCommands?: Step[]
}

/** Runs a script. */
export interface RunScriptParams {
  optional?: boolean
  label?: string
  timeout?: number
  /** Platform restricts this step to a single platform; when set and it doesn't match the running driver, the step is skipped (Maestro #1353). */
  platform?: string
  /** Script content or filename (string form) */
  script?: string
  /** Script filename (map form) */
  file?: string
  env?: Record<string, string>
}

/** Runs a command on the machine driving the test — the host, not the device. That is what makes it useful: it is the escape hatch for adb, simctl, xcrun and the rest of the platform tooling a flow occasionally needs and the runner deliberately does not wrap. */
export interface RunShellParams {
  optional?: boolean
  label?: string
  timeout?: number
  /** Platform restricts this step to a single platform; when set and it doesn't match the running driver, the step is skipped (Maestro #1353). */
  platform?: string
  /** Command is the shell command line. Handed to `sh -c`, so pipes, redirection and quoting all work as written. */
  command?: string
  /** Output names a flow variable to receive the command's trimmed stdout, making it available to later steps as ${NAME}. */
  output?: string
  /** Env adds variables to the command's environment, on top of the runner's own and the MAESTRO_* values describing the device under test. `timeout` comes from BaseStep and bounds the command; it defaults to 30s here, because a hung shell call must not hang the run. */
  env?: Record<string, string>
}

/** Loads and executes a JS file in a mobile WebView via CDP. */
export interface RunWebViewScriptParams {
  optional?: boolean
  label?: string
  timeout?: number
  /** Platform restricts this step to a single platform; when set and it doesn't match the running driver, the step is skipped (Maestro #1353). */
  platform?: string
  /** Path to JS file */
  file?: string
  /** Environment variables injected as window.__env */
  env?: Record<string, string>
  /** Variable name to store the return value */
  output?: string
}

/** Saves cookies + localStorage + sessionStorage to a JSON file. */
export interface SaveAuthStateParams {
  optional?: boolean
  label?: string
  timeout?: number
  /** Platform restricts this step to a single platform; when set and it doesn't match the running driver, the step is skipped (Maestro #1353). */
  platform?: string
  /** Output file path */
  path?: string
}

/** Scrolls the screen. */
export interface ScrollParams {
  optional?: boolean
  label?: string
  timeout?: number
  /** Platform restricts this step to a single platform; when set and it doesn't match the running driver, the step is skipped (Maestro #1353). */
  platform?: string
  direction?: string
  /** Engine selects the scroll backend on Android. "" (default) and "adb" → adb input swipe (matches upstream Maestro). "agent" → driver's existing on-device gesture path (UIA2 server /appium/gestures/scroll for the uiautomator2 driver, RPC MotionEvent injection for the devicelab driver). Ignored on iOS/web. */
  engine?: string
}

/** Scrolls until element is visible. */
export interface ScrollUntilVisibleParams {
  optional?: boolean
  label?: string
  timeout?: number
  /** Platform restricts this step to a single platform; when set and it doesn't match the running driver, the step is skipped (Maestro #1353). */
  platform?: string
  element?: Selector
  /** From restricts the scroll gesture to one container, for a screen with an inner list or a horizontal carousel that a full-width swipe would miss. Empty means scroll the screen, which is the usual case. */
  from?: Selector
  direction?: string
  /** Legacy: max scroll attempts */
  maxScrolls?: number
  speed?: number
  visibilityPercentage?: number
  centerElement?: boolean
  waitToSettleTimeoutMs?: number
  /** Engine selects the scroll backend. See ScrollStep.Engine. */
  engine?: string
}

/** Sets airplane mode. */
export interface SetAirplaneModeParams {
  optional?: boolean
  label?: string
  timeout?: number
  /** Platform restricts this step to a single platform; when set and it doesn't match the running driver, the step is skipped (Maestro #1353). */
  platform?: string
  enabled?: unknown
}

/** Sets the clipboard to a specific text value. */
export interface SetClipboardParams {
  optional?: boolean
  label?: string
  timeout?: number
  /** Platform restricts this step to a single platform; when set and it doesn't match the running driver, the step is skipped (Maestro #1353). */
  platform?: string
  text?: string
}

/** Sets browser cookies via CDP. */
export interface SetCookiesParams {
  optional?: boolean
  label?: string
  timeout?: number
  /** Platform restricts this step to a single platform; when set and it doesn't match the running driver, the step is skipped (Maestro #1353). */
  platform?: string
  cookies?: unknown
}

/** Switches the system UI between dark and light appearance. */
export interface SetDarkModeParams {
  optional?: boolean
  label?: string
  timeout?: number
  /** Platform restricts this step to a single platform; when set and it doesn't match the running driver, the step is skipped (Maestro #1353). */
  platform?: string
  enabled?: unknown
}

/** Sets device location. */
export interface SetLocationParams {
  optional?: boolean
  label?: string
  timeout?: number
  /** Platform restricts this step to a single platform; when set and it doesn't match the running driver, the step is skipped (Maestro #1353). */
  platform?: string
  /** String for variable support */
  latitude?: string
  /** String for variable support */
  longitude?: string
}

/** Simulates network throttling or offline mode. */
export interface SetNetworkConditionsParams {
  optional?: boolean
  label?: string
  timeout?: number
  /** Platform restricts this step to a single platform; when set and it doesn't match the running driver, the step is skipped (Maestro #1353). */
  platform?: string
  offline?: boolean
  /** ms */
  latency?: number
  /** KB/s (-1 = no throttle) */
  downloadSpeed?: number
  /** KB/s (-1 = no throttle) */
  uploadSpeed?: number
}

/** Sets device orientation. */
export interface SetOrientationParams {
  optional?: boolean
  label?: string
  timeout?: number
  /** Platform restricts this step to a single platform; when set and it doesn't match the running driver, the step is skipped (Maestro #1353). */
  platform?: string
  /** PORTRAIT, LANDSCAPE */
  orientation?: string
}

/** Sets app permissions. Permission values: "allow", "deny", "unset" Permission shortcuts: location, camera, contacts, phone, microphone, bluetooth, storage, notifications, medialibrary, calendar, sms, all */
export interface SetPermissionsParams {
  optional?: boolean
  label?: string
  timeout?: number
  /** Platform restricts this step to a single platform; when set and it doesn't match the running driver, the step is skipped (Maestro #1353). */
  platform?: string
  appId?: string
  permissions?: Record<string, string>
}

/** Starts recording. */
export interface StartRecordingParams {
  optional?: boolean
  label?: string
  timeout?: number
  /** Platform restricts this step to a single platform; when set and it doesn't match the running driver, the step is skipped (Maestro #1353). */
  platform?: string
  path?: string
}

/** Stops an app. */
export interface StopAppParams {
  optional?: boolean
  label?: string
  timeout?: number
  /** Platform restricts this step to a single platform; when set and it doesn't match the running driver, the step is skipped (Maestro #1353). */
  platform?: string
  appId?: string
}

/** Stops recording. */
export interface StopRecordingParams {
  optional?: boolean
  label?: string
  timeout?: number
  /** Platform restricts this step to a single platform; when set and it doesn't match the running driver, the step is skipped (Maestro #1353). */
  platform?: string
  path?: string
}

export interface SwipeParams {
  optional?: boolean
  label?: string
  timeout?: number
  /** Platform restricts this step to a single platform; when set and it doesn't match the running driver, the step is skipped (Maestro #1353). */
  platform?: string
  /** UP, DOWN, LEFT, RIGHT */
  direction?: string
  /** "x%, y%" */
  start?: string
  /** "x%, y%" */
  end?: string
  /** Absolute X start */
  startX?: number
  /** Absolute Y start */
  startY?: number
  /** Absolute X end */
  endX?: number
  /** Absolute Y end */
  endY?: number
  /** Duration in ms */
  duration?: number
  /** Speed 0-100 */
  speed?: number
  /** Fraction of screen (0-1) for direction swipes; 0 = default */
  distance?: number
  waitToSettleTimeoutMs?: number
}

/** Switches to another browser tab. */
export interface SwitchTabParams {
  optional?: boolean
  label?: string
  timeout?: number
  /** Platform restricts this step to a single platform; when set and it doesn't match the running driver, the step is skipped (Maestro #1353). */
  platform?: string
  /** Switch by label */
  tabLabel?: string
  /** Switch by index (0-based) */
  index?: number
  /** Switch by URL pattern match */
  url?: string
}

/** Takes a screenshot. When CropOn is set, the screenshot is cropped to the bounds of the matched element instead of capturing the whole screen. Mirrors Maestro's takeScreenshot.cropOn (see https://docs.maestro.dev/reference/commands-available/takescreenshot). */
export interface TakeScreenshotParams {
  optional?: boolean
  label?: string
  timeout?: number
  /** Platform restricts this step to a single platform; when set and it doesn't match the running driver, the step is skipped (Maestro #1353). */
  platform?: string
  path?: string
  cropOn?: Selector
}

/** Taps on an element. */
export interface TapOnParams {
  optional?: boolean
  label?: string
  timeout?: number
  /** Platform restricts this step to a single platform; when set and it doesn't match the running driver, the step is skipped (Maestro #1353). */
  platform?: string
  /** Primary selectors */
  text?: string
  /** Resource ID or accessibility ID */
  id?: string
  /** Size matching */
  width?: number
  height?: number
  tolerance?: number
  /** State filters */
  enabled?: boolean
  selected?: boolean
  checked?: boolean
  focused?: boolean
  /** Index for multiple matches (string for variable support) */
  index?: string
  /** Traits (comma-separated string, e.g., "button,heading") */
  traits?: string
  /** CSS selector for web views */
  css?: string
  /** Web-specific selectors */
  placeholder?: string
  /** Match by ARIA role (button, link, tab, etc.) */
  role?: string
  /** Partial text match (contains) */
  textContains?: string
  /** Match links by href attribute */
  href?: string
  /** Match by alt attribute (images) */
  alt?: string
  /** Match by title attribute (tooltips) */
  title?: string
  /** Match by form field name attribute */
  name?: string
  /** Match by data-testid attribute */
  testId?: string
  /** Match text by regex pattern */
  textRegex?: string
  /** Pick Nth match (0-based) when multiple elements match */
  nth?: number
  /** Relative selectors */
  childOf?: Selector
  below?: Selector
  above?: Selector
  leftOf?: Selector
  rightOf?: Selector
  containsChild?: Selector
  containsDescendants?: Selector[]
  /** Visual containment (center point inside anchor bounds) */
  insideOf?: Selector
  retryTapIfNoChange?: boolean
  waitUntilVisible?: boolean
  /** Tap point "x%, y%" */
  point?: string
  /** Swipe start "x%, y%" */
  start?: string
  /** Swipe end "x%, y%" */
  end?: string
  /** Tap repeat count */
  repeat?: number
  /** Delay between repeats (ms) */
  delay?: number
  /** Wait for UI settle (ms) */
  waitToSettleTimeoutMs?: number
  longPress?: boolean
  duration?: number
}

/** Taps on specific coordinates. */
export interface TapOnPointParams {
  optional?: boolean
  label?: string
  timeout?: number
  /** Platform restricts this step to a single platform; when set and it doesn't match the running driver, the step is skipped (Maestro #1353). */
  platform?: string
  x?: number
  y?: number
  point?: string
  longPress?: boolean
  repeat?: number
  duration?: number
  retryTapIfNoChange?: boolean
  waitToSettleTimeoutMs?: number
}

/** Toggles airplane mode. */
export interface ToggleAirplaneModeParams {
  optional?: boolean
  label?: string
  timeout?: number
  /** Platform restricts this step to a single platform; when set and it doesn't match the running driver, the step is skipped (Maestro #1353). */
  platform?: string
}

/** Flips the current appearance. */
export interface ToggleDarkModeParams {
  optional?: boolean
  label?: string
  timeout?: number
  /** Platform restricts this step to a single platform; when set and it doesn't match the running driver, the step is skipped (Maestro #1353). */
  platform?: string
}

/** Simulates travel. */
export interface TravelParams {
  optional?: boolean
  label?: string
  timeout?: number
  /** Platform restricts this step to a single platform; when set and it doesn't match the running driver, the step is skipped (Maestro #1353). */
  platform?: string
  /** "lat, long" */
  points?: string[]
  /** km/h */
  speed?: number
}

/** Sets files on a file input element. */
export interface UploadFileParams {
  optional?: boolean
  label?: string
  timeout?: number
  /** Platform restricts this step to a single platform; when set and it doesn't match the running driver, the step is skipped (Maestro #1353). */
  platform?: string
  /** Primary selectors */
  text?: string
  /** Resource ID or accessibility ID */
  id?: string
  /** Size matching */
  width?: number
  height?: number
  tolerance?: number
  /** State filters */
  enabled?: boolean
  selected?: boolean
  checked?: boolean
  focused?: boolean
  /** Index for multiple matches (string for variable support) */
  index?: string
  /** Traits (comma-separated string, e.g., "button,heading") */
  traits?: string
  /** CSS selector for web views */
  css?: string
  /** Web-specific selectors */
  placeholder?: string
  /** Match by ARIA role (button, link, tab, etc.) */
  role?: string
  /** Partial text match (contains) */
  textContains?: string
  /** Match links by href attribute */
  href?: string
  /** Match by alt attribute (images) */
  alt?: string
  /** Match by title attribute (tooltips) */
  title?: string
  /** Match by form field name attribute */
  name?: string
  /** Match by data-testid attribute */
  testId?: string
  /** Match text by regex pattern */
  textRegex?: string
  /** Pick Nth match (0-based) when multiple elements match */
  nth?: number
  /** Relative selectors */
  childOf?: Selector
  below?: Selector
  above?: Selector
  leftOf?: Selector
  rightOf?: Selector
  containsChild?: Selector
  containsDescendants?: Selector[]
  /** Visual containment (center point inside anchor bounds) */
  insideOf?: Selector
  retryTapIfNoChange?: boolean
  waitUntilVisible?: boolean
  /** Tap point "x%, y%" */
  point?: string
  /** Swipe start "x%, y%" */
  start?: string
  /** Swipe end "x%, y%" */
  end?: string
  /** Tap repeat count */
  repeat?: number
  /** Delay between repeats (ms) */
  delay?: number
  /** Wait for UI settle (ms) */
  waitToSettleTimeoutMs?: number
  /** Single file path */
  path?: string
  /** Multiple file paths */
  paths?: string[]
}

/** Waits for animations. */
export interface WaitForAnimationToEndParams {
  optional?: boolean
  label?: string
  timeout?: number
  /** Platform restricts this step to a single platform; when set and it doesn't match the running driver, the step is skipped (Maestro #1353). */
  platform?: string
}

/** Waits for a browser download to complete. */
export interface WaitForDownloadParams {
  optional?: boolean
  label?: string
  timeout?: number
  /** Platform restricts this step to a single platform; when set and it doesn't match the running driver, the step is skipped (Maestro #1353). */
  platform?: string
  /** Directory to save downloaded file */
  saveTo?: string
  /** Expected filename (optional) */
  assertFilename?: string
}

/** Waits for a specific network request to be made. */
export interface WaitForRequestParams {
  optional?: boolean
  label?: string
  timeout?: number
  /** Platform restricts this step to a single platform; when set and it doesn't match the running driver, the step is skipped (Maestro #1353). */
  platform?: string
  url?: string
  /** Optional: match specific HTTP method */
  method?: string
  /** Variable name to store request body */
  output?: string
}

/** Shape of every command: scalar shorthand key, value-less, compound. */
export interface CommandSpec {
  name: string
  scalar: string | null
  valueLess: boolean
  compound: boolean
  doc: string
}

export const COMMANDS = {
  acceptAlert: { name: 'acceptAlert', scalar: null, valueLess: true, compound: false, doc: 'Accepts a system alert dialog (taps Allow/OK).' },
  addMedia: { name: 'addMedia', scalar: null, valueLess: false, compound: false, doc: 'Adds media files.' },
  assertCondition: { name: 'assertCondition', scalar: null, valueLess: false, compound: false, doc: 'Asserts a condition. Uses a custom UnmarshalYAML because both BaseStep and Condition have a "timeout" yaml tag — Condition.Timeout is the semantically correct one here (controls how long to wait for visible/notVisible condition checks).' },
  assertDarkMode: { name: 'assertDarkMode', scalar: null, valueLess: true, compound: false, doc: 'Asserts the system UI is currently in dark appearance.' },
  assertLightMode: { name: 'assertLightMode', scalar: null, valueLess: true, compound: false, doc: 'Asserts the system UI is currently in light appearance.' },
  assertNoDefectsWithAI: { name: 'assertNoDefectsWithAI', scalar: null, valueLess: false, compound: false, doc: 'Uses AI to check for visual defects.' },
  assertNoJSErrors: { name: 'assertNoJSErrors', scalar: null, valueLess: true, compound: false, doc: 'Asserts that no console errors or uncaught exceptions occurred.' },
  assertNotVisible: { name: 'assertNotVisible', scalar: 'text', valueLess: false, compound: false, doc: 'Asserts element is not visible.' },
  assertScreenshot: { name: 'assertScreenshot', scalar: 'path', valueLess: false, compound: false, doc: 'Compares a screenshot with a reference image.' },
  assertTrue: { name: 'assertTrue', scalar: 'condition', valueLess: false, compound: false, doc: 'Asserts a script condition is true (alias for assertCondition).' },
  assertVisible: { name: 'assertVisible', scalar: 'text', valueLess: false, compound: false, doc: 'Asserts element is visible.' },
  assertWithAI: { name: 'assertWithAI', scalar: 'assertion', valueLess: false, compound: false, doc: 'Uses AI to verify an assertion.' },
  back: { name: 'back', scalar: null, valueLess: true, compound: false, doc: 'Presses back.' },
  blockNetwork: { name: 'blockNetwork', scalar: null, valueLess: false, compound: false, doc: 'Blocks network requests matching URL patterns.' },
  clearConsoleLogs: { name: 'clearConsoleLogs', scalar: null, valueLess: true, compound: false, doc: 'Clears captured browser console logs.' },
  clearKeychain: { name: 'clearKeychain', scalar: null, valueLess: true, compound: false, doc: 'Clears keychain.' },
  clearNetworkMocks: { name: 'clearNetworkMocks', scalar: null, valueLess: true, compound: false, doc: 'Clears all network mocks and blocks.' },
  clearState: { name: 'clearState', scalar: 'appId', valueLess: false, compound: false, doc: 'Clears app state.' },
  closeTab: { name: 'closeTab', scalar: null, valueLess: true, compound: false, doc: 'Closes the current tab and switches to the previous one.' },
  copyTextFrom: { name: 'copyTextFrom', scalar: 'text', valueLess: false, compound: false, doc: 'Copies text from element.' },
  defineVariables: { name: 'defineVariables', scalar: null, valueLess: false, compound: false, doc: 'Defines session variables; the value is a map of NAME: value.' },
  dismissAlert: { name: 'dismissAlert', scalar: null, valueLess: true, compound: false, doc: 'Dismisses a system alert dialog (taps Don\'t Allow/Cancel).' },
  doubleTapOn: { name: 'doubleTapOn', scalar: 'text', valueLess: false, compound: false, doc: 'Double taps on an element (alias for tapOn with repeat=2).' },
  dragAndDrop: { name: 'dragAndDrop', scalar: null, valueLess: false, compound: false, doc: 'SwipeStep performs a swipe gesture.' },
  eraseText: { name: 'eraseText', scalar: 'characters', valueLess: false, compound: false, doc: 'Erases text.' },
  evalBrowserScript: { name: 'evalBrowserScript', scalar: 'script', valueLess: false, compound: false, doc: 'Executes JavaScript in the browser page context (web only). Unlike EvalScriptStep which runs in Maestro\'s internal JS engine, this runs directly in the browser via CDP, with access to window, document, DOM, etc.' },
  evalScript: { name: 'evalScript', scalar: 'script', valueLess: false, compound: false, doc: 'Evaluates JavaScript.' },
  evalWebViewScript: { name: 'evalWebViewScript', scalar: 'script', valueLess: false, compound: false, doc: 'Executes JavaScript in a mobile WebView via CDP. Unlike EvalBrowserScriptStep (desktop browser), this runs in the WebView context of a native Android app.' },
  extendedWaitUntil: { name: 'extendedWaitUntil', scalar: null, valueLess: false, compound: false, doc: 'Waits for a condition.' },
  extractTextWithAI: { name: 'extractTextWithAI', scalar: null, valueLess: false, compound: false, doc: 'Uses AI to extract text from screen.' },
  getConsoleLogs: { name: 'getConsoleLogs', scalar: 'output', valueLess: false, compound: false, doc: 'Retrieves captured browser console logs as JSON.' },
  getCookies: { name: 'getCookies', scalar: 'output', valueLess: false, compound: false, doc: 'Retrieves browser cookies and stores them as JSON.' },
  grantPermissions: { name: 'grantPermissions', scalar: null, valueLess: false, compound: false, doc: 'Grants browser permissions (notifications, camera, etc).' },
  hideKeyboard: { name: 'hideKeyboard', scalar: null, valueLess: true, compound: false, doc: 'Hides the keyboard.' },
  inputRandom: { name: 'inputRandom', scalar: 'type', valueLess: false, compound: false, doc: 'Generates random input.' },
  inputRandomEmail: { name: 'inputRandomEmail', scalar: null, valueLess: true, compound: false, doc: 'Generates random input.' },
  inputRandomNumber: { name: 'inputRandomNumber', scalar: null, valueLess: true, compound: false, doc: 'Generates random input.' },
  inputRandomPersonName: { name: 'inputRandomPersonName', scalar: null, valueLess: true, compound: false, doc: 'Generates random input.' },
  inputRandomText: { name: 'inputRandomText', scalar: null, valueLess: true, compound: false, doc: 'Generates random input.' },
  inputText: { name: 'inputText', scalar: 'text', valueLess: false, compound: false, doc: 'Inputs text.' },
  killApp: { name: 'killApp', scalar: 'appId', valueLess: false, compound: false, doc: 'Kills an app.' },
  launchApp: { name: 'launchApp', scalar: 'appId', valueLess: false, compound: false, doc: 'Launches an app.' },
  loadAuthState: { name: 'loadAuthState', scalar: 'path', valueLess: false, compound: false, doc: 'Loads cookies + localStorage + sessionStorage from a JSON file.' },
  longPressOn: { name: 'longPressOn', scalar: 'text', valueLess: false, compound: false, doc: 'Long presses on an element (alias for tapOn with longPress=true).' },
  mockNetwork: { name: 'mockNetwork', scalar: null, valueLess: false, compound: false, doc: 'Mocks API responses matching URL pattern and method.' },
  openBrowser: { name: 'openBrowser', scalar: 'url', valueLess: false, compound: false, doc: 'Opens a URL in the browser.' },
  openLink: { name: 'openLink', scalar: 'link', valueLess: false, compound: false, doc: 'Opens a URL.' },
  openNotifications: { name: 'openNotifications', scalar: null, valueLess: true, compound: false, doc: 'Pulls down the Android notification shade. Android-only (no-op on iOS).' },
  openTab: { name: 'openTab', scalar: 'url', valueLess: false, compound: false, doc: 'Opens a new browser tab.' },
  pasteText: { name: 'pasteText', scalar: null, valueLess: true, compound: false, doc: 'Pastes text.' },
  pressKey: { name: 'pressKey', scalar: 'key', valueLess: false, compound: false, doc: 'Presses a key.' },
  removeMedia: { name: 'removeMedia', scalar: null, valueLess: true, compound: false, doc: 'Clears media added by addMedia (Android: MediaStore index).' },
  repeat: { name: 'repeat', scalar: null, valueLess: false, compound: true, doc: 'Repeats steps.' },
  resetPermissions: { name: 'resetPermissions', scalar: null, valueLess: true, compound: false, doc: 'Resets all browser permissions.' },
  retry: { name: 'retry', scalar: null, valueLess: false, compound: true, doc: 'Retries steps on failure.' },
  runBrowserScript: { name: 'runBrowserScript', scalar: 'file', valueLess: false, compound: false, doc: 'Loads and executes a JS file in the browser page context.' },
  runFlow: { name: 'runFlow', scalar: 'file', valueLess: false, compound: true, doc: 'Runs another flow.' },
  runScript: { name: 'runScript', scalar: 'script', valueLess: false, compound: false, doc: 'Runs a script.' },
  runShell: { name: 'runShell', scalar: 'command', valueLess: false, compound: false, doc: 'Runs a command on the machine driving the test — the host, not the device. That is what makes it useful: it is the escape hatch for adb, simctl, xcrun and the rest of the platform tooling a flow occasionally needs and the runner deliberately does not wrap.' },
  runWebViewScript: { name: 'runWebViewScript', scalar: 'file', valueLess: false, compound: false, doc: 'Loads and executes a JS file in a mobile WebView via CDP.' },
  saveAuthState: { name: 'saveAuthState', scalar: 'path', valueLess: false, compound: false, doc: 'Saves cookies + localStorage + sessionStorage to a JSON file.' },
  scroll: { name: 'scroll', scalar: 'direction', valueLess: false, compound: false, doc: 'Scrolls the screen.' },
  scrollUntilVisible: { name: 'scrollUntilVisible', scalar: 'element.text', valueLess: false, compound: false, doc: 'Scrolls until element is visible.' },
  setAirplaneMode: { name: 'setAirplaneMode', scalar: 'enabled', valueLess: false, compound: false, doc: 'Sets airplane mode.' },
  setClipboard: { name: 'setClipboard', scalar: 'text', valueLess: false, compound: false, doc: 'Sets the clipboard to a specific text value.' },
  setCookies: { name: 'setCookies', scalar: null, valueLess: false, compound: false, doc: 'Sets browser cookies via CDP.' },
  setDarkMode: { name: 'setDarkMode', scalar: 'enabled', valueLess: false, compound: false, doc: 'Switches the system UI between dark and light appearance.' },
  setLocation: { name: 'setLocation', scalar: null, valueLess: false, compound: false, doc: 'Sets device location.' },
  setNetworkConditions: { name: 'setNetworkConditions', scalar: null, valueLess: false, compound: false, doc: 'Simulates network throttling or offline mode.' },
  setOrientation: { name: 'setOrientation', scalar: 'orientation', valueLess: false, compound: false, doc: 'Sets device orientation.' },
  setPermissions: { name: 'setPermissions', scalar: null, valueLess: false, compound: false, doc: 'Sets app permissions. Permission values: "allow", "deny", "unset" Permission shortcuts: location, camera, contacts, phone, microphone, bluetooth, storage, notifications, medialibrary, calendar, sms, all' },
  startRecording: { name: 'startRecording', scalar: 'path', valueLess: false, compound: false, doc: 'Starts recording.' },
  stopApp: { name: 'stopApp', scalar: 'appId', valueLess: false, compound: false, doc: 'Stops an app.' },
  stopRecording: { name: 'stopRecording', scalar: 'path', valueLess: false, compound: false, doc: 'Stops recording.' },
  swipe: { name: 'swipe', scalar: 'direction', valueLess: false, compound: false, doc: '' },
  switchTab: { name: 'switchTab', scalar: 'tabLabel', valueLess: false, compound: false, doc: 'Switches to another browser tab.' },
  takeScreenshot: { name: 'takeScreenshot', scalar: 'path', valueLess: false, compound: false, doc: 'Takes a screenshot. When CropOn is set, the screenshot is cropped to the bounds of the matched element instead of capturing the whole screen. Mirrors Maestro\'s takeScreenshot.cropOn (see https://docs.maestro.dev/reference/commands-available/takescreenshot).' },
  tapOn: { name: 'tapOn', scalar: 'text', valueLess: false, compound: false, doc: 'Taps on an element.' },
  tapOnPoint: { name: 'tapOnPoint', scalar: null, valueLess: false, compound: false, doc: 'Taps on specific coordinates.' },
  toggleAirplaneMode: { name: 'toggleAirplaneMode', scalar: null, valueLess: true, compound: false, doc: 'Toggles airplane mode.' },
  toggleDarkMode: { name: 'toggleDarkMode', scalar: null, valueLess: true, compound: false, doc: 'Flips the current appearance.' },
  travel: { name: 'travel', scalar: null, valueLess: false, compound: false, doc: 'Simulates travel.' },
  uploadFile: { name: 'uploadFile', scalar: null, valueLess: false, compound: false, doc: 'Sets files on a file input element.' },
  waitForAnimationToEnd: { name: 'waitForAnimationToEnd', scalar: null, valueLess: false, compound: false, doc: 'Waits for animations.' },
  waitForDownload: { name: 'waitForDownload', scalar: 'saveTo', valueLess: false, compound: false, doc: 'Waits for a browser download to complete.' },
  waitForRequest: { name: 'waitForRequest', scalar: 'url', valueLess: false, compound: false, doc: 'Waits for a specific network request to be made.' },
} as const satisfies Record<string, CommandSpec>

export type CommandName = keyof typeof COMMANDS

/** Params type per command name. */
export interface CommandParams {
  acceptAlert: AcceptAlertParams
  addMedia: AddMediaParams
  assertCondition: AssertConditionParams
  assertDarkMode: AssertDarkModeParams
  assertLightMode: AssertLightModeParams
  assertNoDefectsWithAI: AssertNoDefectsWithAIParams
  assertNoJSErrors: AssertNoJSErrorsParams
  assertNotVisible: AssertNotVisibleParams
  assertScreenshot: AssertScreenshotParams
  assertTrue: AssertTrueParams
  assertVisible: AssertVisibleParams
  assertWithAI: AssertWithAIParams
  back: BackParams
  blockNetwork: BlockNetworkParams
  clearConsoleLogs: ClearConsoleLogsParams
  clearKeychain: ClearKeychainParams
  clearNetworkMocks: ClearNetworkMocksParams
  clearState: ClearStateParams
  closeTab: CloseTabParams
  copyTextFrom: CopyTextFromParams
  defineVariables: DefineVariablesParams
  dismissAlert: DismissAlertParams
  doubleTapOn: DoubleTapOnParams
  dragAndDrop: DragAndDropParams
  eraseText: EraseTextParams
  evalBrowserScript: EvalBrowserScriptParams
  evalScript: EvalScriptParams
  evalWebViewScript: EvalWebViewScriptParams
  extendedWaitUntil: ExtendedWaitUntilParams
  extractTextWithAI: ExtractTextWithAIParams
  getConsoleLogs: GetConsoleLogsParams
  getCookies: GetCookiesParams
  grantPermissions: GrantPermissionsParams
  hideKeyboard: HideKeyboardParams
  inputRandom: InputRandomParams
  inputRandomEmail: InputRandomEmailParams
  inputRandomNumber: InputRandomNumberParams
  inputRandomPersonName: InputRandomPersonNameParams
  inputRandomText: InputRandomTextParams
  inputText: InputTextParams
  killApp: KillAppParams
  launchApp: LaunchAppParams
  loadAuthState: LoadAuthStateParams
  longPressOn: LongPressOnParams
  mockNetwork: MockNetworkParams
  openBrowser: OpenBrowserParams
  openLink: OpenLinkParams
  openNotifications: OpenNotificationsParams
  openTab: OpenTabParams
  pasteText: PasteTextParams
  pressKey: PressKeyParams
  removeMedia: RemoveMediaParams
  repeat: RepeatParams
  resetPermissions: ResetPermissionsParams
  retry: RetryParams
  runBrowserScript: RunBrowserScriptParams
  runFlow: RunFlowParams
  runScript: RunScriptParams
  runShell: RunShellParams
  runWebViewScript: RunWebViewScriptParams
  saveAuthState: SaveAuthStateParams
  scroll: ScrollParams
  scrollUntilVisible: ScrollUntilVisibleParams
  setAirplaneMode: SetAirplaneModeParams
  setClipboard: SetClipboardParams
  setCookies: SetCookiesParams
  setDarkMode: SetDarkModeParams
  setLocation: SetLocationParams
  setNetworkConditions: SetNetworkConditionsParams
  setOrientation: SetOrientationParams
  setPermissions: SetPermissionsParams
  startRecording: StartRecordingParams
  stopApp: StopAppParams
  stopRecording: StopRecordingParams
  swipe: SwipeParams
  switchTab: SwitchTabParams
  takeScreenshot: TakeScreenshotParams
  tapOn: TapOnParams
  tapOnPoint: TapOnPointParams
  toggleAirplaneMode: ToggleAirplaneModeParams
  toggleDarkMode: ToggleDarkModeParams
  travel: TravelParams
  uploadFile: UploadFileParams
  waitForAnimationToEnd: WaitForAnimationToEndParams
  waitForDownload: WaitForDownloadParams
  waitForRequest: WaitForRequestParams
}
